use aiken/transaction/value.{Value}

pub fn unvalue(v: Value) -> List<(ByteArray, List<(ByteArray, Int)>)> {
  let v1: Data =
    v
  expect v2: List<(ByteArray, List<(ByteArray, Int)>)> =
    v1
  v2
}
